<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.game.mobileappar">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera.ar" />

    <application
        android:name=".context.InternalContextApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_netman"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".containers.auth.AuthActivity"
            android:exported="false"
            android:label="@string/title_activity_auth"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".containers.messenger.MessengerActivity" /> <!-- <activity -->
        <!-- android:name=".activities.messenger.MessengerActivity" -->
        <!-- android:exported="false" /> -->
        <!-- &lt;!&ndash;        <activity&ndash;&gt; -->
        <!-- &lt;!&ndash;            android:name=".PhoneActivity"&ndash;&gt; -->
        <!-- &lt;!&ndash;            android:exported="false"&ndash;&gt; -->
        <!-- &lt;!&ndash;            android:label="@string/title_activity_phone"&ndash;&gt; -->
        <!-- &lt;!&ndash;            android:theme="@style/NoActionBar" />&ndash;&gt; -->
        <service
            android:name=".service.NotificationService"
            android:enabled="true"
            android:exported="true" />
        <!--
            Ключ API для API на основе Карт Google определяется как строковый ресурс.
            (файл "res/values/google_maps_api.xml").
            Ключ API связан с ключом шифрования, используемым для подписи APK.
            Нужен другой ключ API для каждого ключа шифрования, включая ключ выпуска, который используется для
            подписи APK для публикации.
            Можно определить ключи для целей отладки и выпуска в src/debug/ и
        -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDuy-N5rMjQkzwQ0OX5gUWWGZf_F4hneBM" />
        <!--
            Активность авторизации появляется сразу после загрузки приложения.
            В случае, если пользователь уже ранее был зарегистрирован, то происходит
            попытка обращения к локальному хранилищу и токен доступа с email будут
            отправлены на сервер для подтверждения личности и тогда пользователю
            не будет необходимости ещё раз авторизовываться, однако если токен
            будет не валидный или по данному email адресу не будет зарегистрировано человека,
            то будет необходимость в повторной авторизации
        -->
        <!-- .LendingActivity -->
        <activity
            android:name=".LendingActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AuthActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".VideoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".RegActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".MainActivity"
            android:fitsSystemWindows="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"
            android:windowNoTitle="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="com.canhub.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>